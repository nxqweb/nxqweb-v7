import { useEffect, useMemo, useState } from "react";
import { AlertTriangle, ArrowLeft, CheckCircle2, Rocket, ShieldAlert } from "lucide-react";
import { isSupabaseConfigured, supabase } from "../lib/supabaseClient";

type Check={id:string;check_key:string;category:string;title:string;required:boolean;status:string;evidence:Record<string,unknown>;last_checked_at:string|null;checked_by:string|null};
export function OwnerLaunchReadiness(){const [checks,setChecks]=useState<Check[]>([]);const [error,setError]=useState("");async function load(){if(!isSupabaseConfigured||!supabase)return;const r=await supabase.from("launch_readiness_checks").select("*").order("category").order("title");if(r.error){setError(r.error.message);return;}setChecks((r.data||[]) as Check[]);}useEffect(()=>{void load();},[]);const required=checks.filter(c=>c.required);const ready=required.filter(c=>c.status==="ready").length;const blocked=required.filter(c=>c.status==="blocked"||c.status==="unknown").length;const pct=required.length?Math.round(ready/required.length*100):0;return <main className="nxq-page"><section className="portal-shell"><div className="panel-title panel-title-row"><div className="panel-title"><Rocket size={22}/><div><h1>Launch readiness</h1><p className="subtle">Evidence-driven launch gate. This never replaces your explicit final launch approval.</p></div></div><a className="icon-btn" href="/owner"><ArrowLeft size={16}/> Owner</a></div>{error?<div className="auth-error">{error}</div>:null}<div className="portal-grid"><section className="panel"><h2>Required ready</h2><div className="status-summary">{ready}/{required.length}</div></section><section className="panel"><h2>Readiness</h2><div className="status-summary">{pct}%</div></section><section className="panel"><h2>Blocking/unknown</h2><div className="status-summary">{blocked}</div></section></div><section className="panel panel-wide">{checks.map(c=><article className="owner-message-card" key={c.id}><div className="panel-title panel-title-row"><div className="panel-title">{c.status==="ready"?<CheckCircle2 size={18}/>:c.status==="warning"?<AlertTriangle size={18}/>:<ShieldAlert size={18}/>}<div><strong>{c.title}</strong><p className="subtle">{c.category} · {c.check_key} · {c.required?"required":"optional"}</p></div></div><span className="status-summary">{c.status.replaceAll("_"," ")}</span></div>{c.last_checked_at?<p className="subtle">Checked {new Date(c.last_checked_at).toLocaleString()} {c.checked_by?`by ${c.checked_by}`:""}</p>:null}</article>)}</section></section></main>;}