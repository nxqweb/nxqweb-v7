import { useEffect, useMemo, useState } from "react";
import { Activity, ArrowLeft, Clock3, ServerCog } from "lucide-react";
import { isSupabaseConfigured, supabase } from "../lib/supabaseClient";

type Worker={worker_key:string;execution_target:string;status:string;worker_version:string|null;queue_depth:number|null;oldest_job_age_seconds:number|null;average_job_ms:number|null;last_job_type:string|null;last_success_at:string|null;last_error_at:string|null;last_error:string|null;heartbeat_at:string};
type Job={execution_target:string;status:string;created_at:string;run_after:string};
export function OwnerAutomationHealth(){const [workers,setWorkers]=useState<Worker[]>([]);const [jobs,setJobs]=useState<Job[]>([]);const [error,setError]=useState("");useEffect(()=>{let active=true;async function load(){if(!isSupabaseConfigured||!supabase)return;const [w,j]=await Promise.all([supabase.from("automation_worker_heartbeats").select("*").order("worker_key"),supabase.from("automation_jobs").select("execution_target,status,created_at,run_after").in("status",["queued","running","failed","blocked"]).order("created_at",{ascending:true}).limit(500)]);if(!active)return;if(w.error||j.error){setError(w.error?.message||j.error?.message||"Automation health could not load.");return;}setWorkers((w.data||[]) as Worker[]);setJobs((j.data||[]) as Job[]);}void load();return()=>{active=false;};},[]);const queue=useMemo(()=>jobs.reduce((acc,j)=>{const key=`${j.execution_target}:${j.status}`;acc[key]=(acc[key]||0)+1;return acc;},{} as Record<string,number>),[jobs]);const oldest=jobs.length?Math.max(0,Math.round((Date.now()-new Date(jobs[0].created_at).getTime())/60000)):0;return <main className="nxq-page"><section className="portal-shell"><div className="panel-title panel-title-row"><div className="panel-title"><ServerCog size={22}/><div><h1>Automation health</h1><p className="subtle">Worker heartbeats, queue depth, stalled work, and execution-lane health.</p></div></div><a className="icon-btn" href="/owner"><ArrowLeft size={16}/> Owner</a></div>{error?<div className="auth-error">{error}</div>:null}<div className="portal-grid"><section className="panel"><Activity size={20}/><h2>Workers</h2><div className="status-summary">{workers.length}</div></section><section className="panel"><Clock3 size={20}/><h2>Open jobs</h2><div className="status-summary">{jobs.length}</div></section><section className="panel"><Clock3 size={20}/><h2>Oldest open</h2><div className="status-summary">{oldest} min</div></section></div><section className="panel panel-wide"><h2>Execution lanes</h2>{["backend","edge","ai"].map(lane=><div className="owner-message-card" key={lane}><strong>{lane}</strong><span className="subtle">queued {queue[`${lane}:queued`]||0} · running {queue[`${lane}:running`]||0} · failed {queue[`${lane}:failed`]||0} · blocked {queue[`${lane}:blocked`]||0}</span></div>)}</section><section className="panel panel-wide"><h2>Workers</h2>{workers.length===0?<div className="empty-state">No heartbeat records yet. Workers will register here when runtime deployment is connected.</div>:workers.map(w=><article className="owner-message-card" key={w.worker_key}><div className="panel-title panel-title-row"><div><strong>{w.worker_key}</strong><p className="subtle">{w.execution_target} · heartbeat {new Date(w.heartbeat_at).toLocaleString()}</p></div><span className="status-summary">{w.status}</span></div><p className="subtle">Queue {w.queue_depth??"—"} · oldest {w.oldest_job_age_seconds??"—"}s · avg {w.average_job_ms??"—"}ms · last job {w.last_job_type||"—"}</p>{w.last_error?<div className="auth-error">{w.last_error}</div>:null}</article>)}</section></section></main>;}