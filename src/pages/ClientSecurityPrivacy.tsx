import { useEffect, useState } from "react";
import { ArrowLeft, Download, KeyRound, ShieldCheck, Trash2 } from "lucide-react";
import { isSupabaseConfigured, supabase } from "../lib/supabaseClient";

type EventRow={id:string;event_type:string;severity:string;trusted:boolean;device_reference:string|null;created_at:string};
type Credential={id:string;credential_type:string;display_name:string|null;assurance_level:number;status:string;last_used_at:string|null;created_at:string};
type Consent={id:string;consent_type:string;policy_version:string;status:string;created_at:string};
type Request={id:string;request_code:string;request_type:string;status:string;requested_at:string;due_at:string|null};
export function ClientSecurityPrivacy(){const [events,setEvents]=useState<EventRow[]>([]);const [credentials,setCredentials]=useState<Credential[]>([]);const [consents,setConsents]=useState<Consent[]>([]);const [requests,setRequests]=useState<Request[]>([]);const [error,setError]=useState("");const [message,setMessage]=useState("");async function load(){if(!isSupabaseConfigured||!supabase)return;const [e,c,p,r]=await Promise.all([supabase.from("account_security_events").select("id,event_type,severity,trusted,device_reference,created_at").order("created_at",{ascending:false}).limit(50),supabase.from("nxq_trusted_credentials").select("id,credential_type,display_name,assurance_level,status,last_used_at,created_at").order("created_at",{ascending:false}),supabase.from("privacy_consents").select("id,consent_type,policy_version,status,created_at").order("created_at",{ascending:false}).limit(50),supabase.from("data_subject_requests").select("id,request_code,request_type,status,requested_at,due_at").order("requested_at",{ascending:false}).limit(20)]);const err=e.error||c.error||p.error||r.error;if(err){setError(err.message);return;}setEvents((e.data||[]) as EventRow[]);setCredentials((c.data||[]) as Credential[]);setConsents((p.data||[]) as Consent[]);setRequests((r.data||[]) as Request[]);}useEffect(()=>{void load();},[]);async function request(type:"export"|"delete"|"restrict"){if(!supabase)return;if(type==="delete"&&!window.confirm("Submit an account/data deletion request? NXQ will verify the request before any destructive action."))return;setError("");setMessage("");const r=await supabase.rpc("submit_current_account_data_request",{target_request_type:type,target_scope:{source:"client_security_privacy_center"}});if(r.error){setError(r.error.message);return;}setMessage(`${type} request submitted. No destructive deletion occurs automatically without the required verification workflow.`);await load();}
return <main className="nxq-page"><section className="portal-shell"><div className="panel-title panel-title-row"><div className="panel-title"><ShieldCheck size={22}/><div><h1>Security & privacy</h1><p className="subtle">NXQ ID security activity, trusted credentials, consent state, and data requests.</p></div></div><a className="icon-btn" href="/client/settings"><ArrowLeft size={16}/> Settings</a></div>{error?<div className="auth-error">{error}</div>:null}{message?<div className="auth-success">{message}</div>:null}<div className="owner-detail-grid"><section className="panel panel-wide"><div className="panel-title"><KeyRound size={20}/><div><h2>Trusted credentials</h2><p className="subtle">Passkeys/security keys store provider/public credential references only — never fingerprints, face scans, or private keys.</p></div></div>{credentials.length===0?<div className="empty-state">No trusted credential metadata is registered yet.</div>:credentials.map(c=><div className="owner-message-card" key={c.id}><strong>{c.display_name||c.credential_type}</strong><span className="subtle">{c.credential_type} · assurance {c.assurance_level} · {c.status}</span></div>)}</section><section className="panel panel-wide"><h2>Recent security activity</h2>{events.length===0?<div className="empty-state">No security events recorded yet.</div>:events.map(e=><div className="owner-message-card" key={e.id}><strong>{e.event_type.replaceAll("_"," ")}</strong><span className="subtle">{e.severity} · {e.trusted?"trusted":"unverified context"} · {new Date(e.created_at).toLocaleString()}</span></div>)}</section><section className="panel panel-wide"><h2>Privacy consent</h2>{consents.length===0?<div className="empty-state">No account-level consent records yet. Visitor analytics consent on managed websites is also kept separate from NXQ account identity.</div>:consents.map(c=><div className="owner-message-card" key={c.id}><strong>{c.consent_type.replaceAll("_"," ")}</strong><span className="subtle">{c.status} · policy {c.policy_version}</span></div>)}</section><section className="panel panel-wide"><h2>Your data</h2><div style={{display:"flex",gap:".6rem",flexWrap:"wrap"}}><button className="wide-btn" onClick={()=>void request("export")}><Download size={16}/> Request export</button><button className="wide-btn" onClick={()=>void request("restrict")}><ShieldCheck size={16}/> Request restriction</button><button className="wide-btn" onClick={()=>void request("delete")}><Trash2 size={16}/> Request deletion</button></div>{requests.map(r=><div className="owner-message-card" key={r.id}><strong>{r.request_code} · {r.request_type}</strong><span className="subtle">{r.status} · submitted {new Date(r.requested_at).toLocaleString()}</span></div>)}</section></div></section></main>;}