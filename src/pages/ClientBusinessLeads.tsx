import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, CircleAlert, Phone, Target } from "lucide-react";
import { isSupabaseConfigured, supabase } from "../lib/supabaseClient";

type Lead={id:string;lead_code:string;status:string;urgency:string;service_key:string|null;contact_name:string|null;contact_email:string|null;contact_phone:string|null;message:string|null;lead_score:number;created_at:string};
const statuses=["new","contacted","qualified","won","lost","spam","archived"];
export function ClientBusinessLeads(){const [leads,setLeads]=useState<Lead[]>([]);const [error,setError]=useState("");const [filter,setFilter]=useState("open");async function load(){if(!isSupabaseConfigured||!supabase)return;const r=await supabase.from("client_leads").select("id,lead_code,status,urgency,service_key,contact_name,contact_email,contact_phone,message,lead_score,created_at").order("created_at",{ascending:false}).limit(250);if(r.error){setError(r.error.message);return;}setLeads((r.data||[]) as Lead[]);}useEffect(()=>{void load();},[]);const visible=useMemo(()=>filter==="all"?leads:filter==="open"?leads.filter(l=>!["won","lost","spam","archived"].includes(l.status)):leads.filter(l=>l.status===filter),[leads,filter]);async function setStatus(lead:Lead,status:string){if(!supabase)return;const r=await supabase.from("client_leads").update({status,contacted_at:status==="contacted"?new Date().toISOString():undefined,converted_at:status==="won"?new Date().toISOString():undefined,updated_at:new Date().toISOString()}).eq("id",lead.id);if(r.error){setError(r.error.message);return;}await load();}
return <main className="nxq-page"><section className="portal-shell"><div className="panel-title panel-title-row"><div className="panel-title"><Target size={22}/><div><h1>Leads</h1><p className="subtle">Website inquiries, urgency, qualification, and conversion status.</p></div></div><a className="icon-btn" href="/client/business"><ArrowLeft size={16}/> Business</a></div>{error?<div className="auth-error">{error}</div>:null}<div className="panel panel-wide"><label>View <select className="auth-input" value={filter} onChange={e=>setFilter(e.target.value)}><option value="open">Open leads</option><option value="all">All leads</option>{statuses.map(s=><option key={s} value={s}>{s.replaceAll("_"," ")}</option>)}</select></label></div><div style={{display:"grid",gap:"1rem"}}>{visible.length===0?<div className="empty-state">No leads in this view yet.</div>:visible.map(lead=><article className="panel" key={lead.id}><div className="panel-title panel-title-row"><div><strong>{lead.contact_name||"Website visitor"}</strong><p className="subtle">{lead.lead_code} · {new Date(lead.created_at).toLocaleString()} · Score {lead.lead_score}</p></div>{["urgent","emergency"].includes(lead.urgency)?<span className="status-summary"><CircleAlert size={15}/> {lead.urgency}</span>:null}</div>{lead.service_key?<p><strong>Service:</strong> {lead.service_key}</p>:null}{lead.message?<p>{lead.message}</p>:null}<p className="subtle">{lead.contact_email||""}{lead.contact_email&&lead.contact_phone?" · ":""}{lead.contact_phone||""}</p><div style={{display:"flex",gap:".6rem",flexWrap:"wrap"}}>{lead.contact_phone?<a className="icon-btn" href={`tel:${lead.contact_phone}`}><Phone size={15}/> Call</a>:null}<select className="auth-input" value={lead.status} onChange={e=>void setStatus(lead,e.target.value)}>{statuses.map(s=><option key={s} value={s}>{s}</option>)}</select></div></article>)}</div></section></main>;}