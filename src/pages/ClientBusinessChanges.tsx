import { useEffect, useState } from "react";
import { ArrowLeft, CheckCircle2, MessageSquarePlus } from "lucide-react";
import { isSupabaseConfigured, supabase } from "../lib/supabaseClient";

type RequestRow={id:string;request_code:string;request_type:string;title:string;priority:string;risk_level:string;status:string;preview_url:string|null;published_url:string|null;last_error:string|null;created_at:string};
export function ClientBusinessChanges(){const [projectId,setProjectId]=useState("");const [rows,setRows]=useState<RequestRow[]>([]);const [type,setType]=useState("content");const [title,setTitle]=useState("");const [description,setDescription]=useState("");const [priority,setPriority]=useState("normal");const [phone,setPhone]=useState("");const [email,setEmail]=useState("");const [service,setService]=useState("");const [error,setError]=useState("");const [message,setMessage]=useState("");const [sending,setSending]=useState(false);async function load(){if(!isSupabaseConfigured||!supabase)return;const session=await supabase.auth.getSession();const user=session.data.session?.user;if(!user)return;const client=await supabase.from("clients").select("id").eq("auth_user_id",user.id).maybeSingle();if(client.error||!client.data){setError(client.error?.message||"Client account not found.");return;}const project=await supabase.from("projects").select("id").eq("client_id",client.data.id).order("created_at",{ascending:false}).limit(1).maybeSingle();if(project.error||!project.data){setError(project.error?.message||"Project not ready yet.");return;}setProjectId(project.data.id);const req=await supabase.from("website_change_requests").select("id,request_code,request_type,title,priority,risk_level,status,preview_url,published_url,last_error,created_at").eq("project_id",project.data.id).order("created_at",{ascending:false}).limit(100);if(req.error){setError(req.error.message);return;}setRows((req.data||[]) as RequestRow[]);}useEffect(()=>{void load();},[]);async function submit(){if(!supabase||!projectId)return;setSending(true);setError("");setMessage("");const patch:Record<string,unknown>={};if(phone.trim())patch.contact_phone=phone.trim();if(email.trim())patch.contact_email=email.trim();if(service.trim())patch.add_services=service.split(",").map(v=>v.trim()).filter(Boolean);const r=await supabase.rpc("submit_current_client_change_request",{target_project_id:projectId,target_request_type:type,target_title:title,target_description:description,target_priority:priority,target_payload:{patch}});setSending(false);if(r.error){setError(r.error.message);return;}setMessage("Change request submitted. NXQ will route safe changes automatically and escalate anything that needs extra review.");setTitle("");setDescription("");setPhone("");setEmail("");setService("");await load();}
return <main className="nxq-page"><section className="portal-shell"><div className="panel-title panel-title-row"><div className="panel-title"><MessageSquarePlus size={22}/><div><h1>Website changes</h1><p className="subtle">Request updates without emailing back and forth. NXQ routes safe changes through a protected build and preview path.</p></div></div><a className="icon-btn" href="/client/business"><ArrowLeft size={16}/> Business</a></div>{error?<div className="auth-error">{error}</div>:null}{message?<div className="auth-success"><CheckCircle2 size={16}/> {message}</div>:null}<section className="panel panel-wide"><h2>New request</h2><div className="setup-form-grid"><label><span>Type</span><select className="auth-input" value={type} onChange={e=>setType(e.target.value)}>{["content","image","service","pricing","new_page","domain","seo","design","location","other"].map(v=><option key={v} value={v}>{v.replaceAll("_"," ")}</option>)}</select></label><label><span>Priority</span><select className="auth-input" value={priority} onChange={e=>setPriority(e.target.value)}>{["low","normal","high","urgent"].map(v=><option key={v}>{v}</option>)}</select></label><label><span>Title</span><input className="auth-input" value={title} maxLength={160} onChange={e=>setTitle(e.target.value)}/></label><label><span>Description</span><textarea className="auth-input" value={description} maxLength={6000} rows={5} onChange={e=>setDescription(e.target.value)}/></label><label><span>New phone (optional structured change)</span><input className="auth-input" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label><span>New email (optional structured change)</span><input className="auth-input" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label><span>Add services (comma separated)</span><input className="auth-input" value={service} onChange={e=>setService(e.target.value)}/></label></div><button className="wide-btn" disabled={sending||!projectId||title.trim().length<3||description.trim().length<5} onClick={()=>void submit()}>{sending?"Submitting…":"Submit change request"}</button></section><section className="panel panel-wide"><h2>Request history</h2>{rows.length===0?<div className="empty-state">No change requests yet.</div>:<div style={{display:"grid",gap:".8rem"}}>{rows.map(row=><article className="owner-message-card" key={row.id}><div className="panel-title panel-title-row"><div><strong>{row.title}</strong><p className="subtle">{row.request_code} · {row.request_type} · {row.risk_level} risk · {new Date(row.created_at).toLocaleString()}</p></div><span className="status-summary">{row.status.replaceAll("_"," ")}</span></div>{row.last_error?<p className="auth-error">{row.last_error}</p>:null}{row.preview_url?<a className="icon-btn" href={row.preview_url} target="_blank" rel="noreferrer">Preview</a>:null}{row.published_url?<a className="icon-btn" href={row.published_url} target="_blank" rel="noreferrer">Published site</a>:null}</article>)}</div>}</section></section></main>;}